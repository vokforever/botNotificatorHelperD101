# Логирование запуска и остановки бота

## Описание

Бот теперь автоматически отправляет уведомления в чат при запуске и остановке, включая подробную статистику работы.

## Функциональность

### 🚀 Уведомление о запуске
При запуске бота автоматически отправляется сообщение с информацией:
- Время запуска
- Количество отслеживаемых сервисов
- Количество пользователей в базе
- Количество активных фильтров
- Количество релизов в истории

**Пример сообщения:**
```
🚀 Бот успешно запущен!

⏰ Время запуска: 2025-08-18 06:53:02
📦 Отслеживается репозиториев: 11
👥 Пользователей в базе: 1
🔍 Пользователей с фильтрами: 0
📈 Релизов в истории: 6

Бот готов к работе! 🎉
```

### 🛑 Уведомление об остановке
При остановке бота отправляется сообщение с итоговой статистикой:
- Время остановки
- Общее время работы
- Количество выполненных проверок
- Количество отправленных уведомлений

**Пример сообщения:**
```
🛑 Бот остановлен

⏰ Время остановки: 2025-08-18 07:03:19
📊 Время работы: 0д 16ч 8м
📈 Всего проверок: 22
🔔 Всего уведомлений: 1

До свидания! 👋
```

## Настройка

### 1. Переменные окружения
Убедитесь, что в файле `.env` установлен `ADMIN_ID`:
```env
ADMIN_ID=123456789
```

### 2. Права доступа
Бот будет отправлять уведомления только в чат с указанным `ADMIN_ID`.

## Использование

### Автоматическое логирование
Логирование происходит автоматически:
- **При запуске** - сразу после инициализации бота
- **При остановке** - при получении сигнала SIGINT/SIGTERM или ошибке

### Ручное тестирование
Используйте команду `/test_logging` для тестирования функций логирования:
```
/test_logging
```

Эта команда:
1. Отправляет тестовое уведомление о запуске
2. Ждет 2 секунды
3. Отправляет тестовое уведомление об остановке

## Технические детали

### Обработка сигналов
Бот корректно обрабатывает сигналы остановки:
- `SIGINT` (Ctrl+C)
- `SIGTERM` (сигнал завершения)

### Статистика
Бот отслеживает:
- Количество проверок сервисов
- Количество отправленных уведомлений
- Время работы

### Асинхронность
Все функции логирования работают асинхронно и не блокируют основную работу бота.

## Файлы

- `main.py` - основной файл с функциями логирования
- `test_bot_logging.py` - тестовый скрипт для проверки функций
- `README-LOGGING.md` - этот файл с документацией

## Команды

| Команда | Описание |
|---------|----------|
| `/start` | Запуск бота |
| `/help` | Справка по использованию |
| `/test_groq` | Тестирование Groq API |
| `/test_logging` | Тестирование логирования |

## Примеры использования

### Запуск бота
```bash
python main.py
```

### Тестирование логирования
```bash
python test_bot_logging.py
```

### Тестирование через Telegram
Отправьте команду `/test_logging` в чат с ботом.

## Устранение неполадок

### Бот не отправляет уведомления
1. Проверьте, что `ADMIN_ID` установлен в `.env`
2. Убедитесь, что бот имеет доступ к чату
3. Проверьте логи на наличие ошибок

### Ошибки при компиляции
1. Убедитесь, что все зависимости установлены
2. Проверьте синтаксис Python
3. Убедитесь, что используется Python 3.7+

## Зависимости

- `python-telegram-bot` - для работы с Telegram API
- `supabase` - для работы с базой данных
- `python-dotenv` - для загрузки переменных окружения
- `asyncio` - для асинхронной работы
- `signal` - для обработки сигналов
