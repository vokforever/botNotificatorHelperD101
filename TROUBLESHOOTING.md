# 🔧 Руководство по устранению неполадок

## Проблема: Данные не сохраняются в базу данных при нажатии "ДА"

### 🔍 Диагностика

1. **Запустите тест подключения к базе данных:**
   ```bash
   python test_db_connection.py
   ```

2. **Проверьте логи бота** - теперь добавлено подробное логирование

### 🚨 Основные причины и решения

#### 1. Отсутствует файл `.env`
**Симптом:** Бот не может подключиться к Supabase

**Решение:**
1. Создайте файл `.env` в корневой папке проекта
2. Скопируйте содержимое из `env.example`
3. Заполните реальными значениями:

```env
# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Groq AI Configuration
GROQ_API_KEY=gsk_...

# Admin Configuration
ADMIN_ID=123456789

# Application Settings
USE_LOCAL=false
```

#### 2. Неправильные настройки Supabase
**Симптом:** Ошибка подключения к базе данных

**Решение:**
1. Проверьте `SUPABASE_URL` - должен начинаться с `https://`
2. Проверьте `SUPABASE_KEY` - используйте anon key, не service_role key
3. Убедитесь, что проект Supabase активен

#### 3. Проблемы с таблицей базы данных
**Симптом:** Ошибка при вставке данных

**Решение:**
1. Запустите SQL-скрипт `database_update.sql` в вашей базе данных
2. Убедитесь, что таблица `digital_notificator_services` существует
3. Проверьте права доступа (RLS policies)

### 🧪 Тестирование

#### Шаг 1: Проверка подключения
```bash
python test_db_connection.py
```

#### Шаг 2: Запуск бота с логированием
```bash
python main.py
```

#### Шаг 3: Отправка тестового сообщения
Отправьте боту сообщение с данными об оплате

#### Шаг 4: Анализ логов
В консоли должны появиться DEBUG сообщения:
- `🔍 DEBUG: Получен callback: save_parsed:...`
- `🔍 DEBUG: Начинаем сохранение данных...`
- `🔍 DEBUG: Проверяем подключение к Supabase...`

### 📋 Структура таблицы

Убедитесь, что таблица содержит все необходимые поля:

```sql
CREATE TABLE digital_notificator_services (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255),
    expires_at DATE,
    user_id BIGINT,
    status VARCHAR(20) DEFAULT 'active',
    description TEXT,
    cost DECIMAL(10,2),
    project VARCHAR(255),
    provider VARCHAR(255),
    parsing_method VARCHAR(50),
    created_at TIMESTAMP DEFAULT NOW()
);
```

### 🔑 Получение Supabase ключей

1. Войдите в [Supabase Dashboard](https://app.supabase.com)
2. Выберите ваш проект
3. Перейдите в Settings → API
4. Скопируйте:
   - **Project URL** → `SUPABASE_URL`
   - **anon public** → `SUPABASE_KEY`

### 📱 Получение Telegram Bot Token

1. Напишите [@BotFather](https://t.me/botfather) в Telegram
2. Используйте команду `/newbot` или `/mybots`
3. Скопируйте полученный токен

### 🚀 После исправления

1. Перезапустите бота
2. Проверьте логи на наличие ошибок
3. Попробуйте снова отправить сообщение
4. Нажмите "ДА" для сохранения

### 📞 Если проблема остается

1. Проверьте все логи в консоли
2. Убедитесь, что все переменные окружения установлены
3. Проверьте подключение к интернету
4. Убедитесь, что Supabase проект активен и доступен
