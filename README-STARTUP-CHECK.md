# Проверка проектов на старте бота

## Описание функциональности

Бот автоматически проверяет все активные сервисы при запуске и отправляет уведомление о тех, что скоро закончатся или уже закончились. Для каждого проблемного сервиса предоставляются кнопки управления.

## Основные возможности

### 🔍 Автоматическая проверка при запуске
- Проверяет все активные сервисы со статусом "active"
- Находит сервисы, которые истекают в течение 30 дней
- Находит сервисы, которые уже истекли
- Отправляет уведомление админу с кнопками управления

### 🎯 Кнопки управления для каждого сервиса

#### 💰 "Оплатили"
- Отмечает сервис как оплаченный
- Устанавливает статус "paid"
- Записывает дату оплаты
- Сервис больше не будет появляться в уведомлениях

#### 📅 "Продли на год"
- Автоматически продлевает сервис на год
- Обновляет дату окончания в базе данных
- Возвращает сервис в активные
- Сбрасывает уведомления

### 🚀 Массовые операции

#### 💰 "Все оплачены"
- Отмечает все проблемные сервисы как оплаченные
- Применяется ко всем сервисам, которые скоро закончатся или уже закончились

#### 📅 "Продлить все хостинги"
- Продлевает все хостинги/домены на год
- Применяется только к сервисам, определенным как хостинг или домен

## Автоматическое определение хостинга/домена

Бот автоматически определяет, является ли сервис хостингом или доменом по следующим критериям:

### По провайдеру:
- `хостинг-провайдер`
- `доменный регистратор`
- `хостинг`

### По названию:
- Содержит слово "хостинг"
- Содержит слово "домен"

### По формату:
- Домены содержат точку (например: `example.com`)

## Техническая реализация

### Основные функции:
- `check_expiring_projects_on_startup()` - основная функция проверки
- `send_startup_expiry_notification()` - отправка уведомления с кнопками
- Обработчики для всех типов кнопок в `handle_notification_buttons()`

### База данных:
- Обновление поля `expires_at` на текущая дата + 365 дней
- Сброс статуса на "active"
- Очистка полей уведомлений (`last_notification`, `notification_date`)
- Запись даты оплаты (`payment_date`)

### Команды:
- `/check_startup` - принудительная проверка проектов на старте (только для админа)

## Примеры использования

### 1. Автоматическая проверка при запуске
При запуске бота автоматически:
1. Отправляется уведомление о запуске
2. Проверяются все активные сервисы
3. Если есть проблемные сервисы - отправляется уведомление с кнопками

### 2. Принудительная проверка
Используйте команду `/check_startup` для принудительной проверки в любое время.

### 3. Управление отдельными сервисами
Для каждого сервиса доступны кнопки:
- Отметить как оплаченный
- Продлить на год

### 4. Массовые операции
Для экономии времени используйте:
- "Все оплачены" - если все сервисы уже оплачены
- "Продлить все хостинги" - для автоматического продления всех хостингов

## Настройка

### Период проверки
По умолчанию проверяются сервисы, которые истекают в течение 30 дней. Для изменения этого периода отредактируйте код в функции `check_expiring_projects_on_startup()`.

### Уведомления
Уведомления отправляются только админу (ADMIN_ID из .env файла).

## Тестирование

Для тестирования функциональности используйте:
```bash
python test_startup_check.py
```

Этот скрипт:
- Подключается к базе данных
- Проверяет все активные сервисы
- Показывает результаты проверки
- Тестирует определение хостинга/домена

## Безопасность

- Все операции доступны только админу
- Перед массовыми операциями рекомендуется создать резервную копию
- Все изменения логируются в консоль

## Логирование

Бот выводит в консоль:
- Количество проверенных сервисов
- Количество проблемных сервисов
- Результаты операций
- Ошибки при обработке

## Советы по использованию

1. **Регулярно проверяйте** проекты при запуске бота
2. **Используйте массовые операции** для экономии времени
3. **Проверяйте логи** для отслеживания операций
4. **Создавайте резервные копии** перед массовыми изменениями
5. **Используйте команду `/check_startup`** для принудительной проверки

## Возможные улучшения

- [ ] Настройка периода проверки через конфигурацию
- [ ] Уведомления о критических сервисах (истекших более 7 дней назад)
- [ ] Автоматическое создание резервных копий
- [ ] Интеграция с календарем для планирования платежей
- [ ] Уведомления для разных уровней критичности
