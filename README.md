# Bot Notificator Helper

Telegram бот для отслеживания сервисов с системой автоматических уведомлений.

## 🚀 Функциональность

### Система уведомлений
- **За месяц** (30 дней) - первое уведомление
- **За 2 недели** (14 дней) - второе уведомление  
- **За 1 неделю** (7 дней) - третье уведомление
- **За 5 дней** - ежедневные уведомления до окончания

### Управление статусом
- ✅ **"Уведомил, жду оплаты"** - сервис остается в системе отслеживания
- 💰 **"Оплатили"** - сервис убирается из уведомлений

## 📋 Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Создайте файл `.env` с переменными окружения:
```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
SUPABASE_URL=your_supabase_url_here
SUPABASE_KEY=your_supabase_key_here
GROQ_API_KEY=your_groq_api_key_here
ADMIN_ID=123456789
```

3. Обновите структуру базы данных:
```sql
-- Выполните SQL скрипт database_update.sql в вашей базе Supabase
```

## 🗄️ Структура базы данных

Таблица `digital_notificator_services`:
- `id` - уникальный идентификатор
- `name` - название сервиса
- `expires_at` - дата окончания (YYYY-MM-DD)
- `user_id` - ID пользователя Telegram
- `status` - статус: `active`, `notified`, `paid`
- `last_notification` - тип последнего уведомления
- `notification_date` - дата последнего уведомления
- `payment_date` - дата оплаты

## ⚙️ Настройка

### ADMIN_ID
Укажите ваш Telegram ID в переменной `ADMIN_ID` для получения уведомлений.

### Время уведомлений
По умолчанию проверка происходит каждый день в 9:00. Изменить можно в функции `start_notification_scheduler()`.

## 🔧 Запуск

```bash
python main.py
```

Бот автоматически:
- Запустит планировщик уведомлений
- Начнет отслеживать сервисы
- Отправлять уведомления согласно расписанию

## 📱 Использование

1. **Добавление сервиса**: Отправьте скриншот с информацией о сервисе
2. **Получение уведомлений**: Админ получает автоматические уведомления
3. **Управление статусом**: Используйте кнопки в уведомлениях для обновления статуса

## 🚨 Логирование

Все действия логируются в консоль:
- Отправка уведомлений
- Обновление статусов
- Ошибки и исключения

## 📝 Примечания

- Сервисы со статусом `paid` больше не появляются в уведомлениях
- Система автоматически рассчитывает дни до окончания сервиса
- Планировщик работает в фоновом режиме
